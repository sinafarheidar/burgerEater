<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{eat}}">
		{{#if eat}}Eat the buger{{else}}{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>
